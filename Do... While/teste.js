const prompt = require("prompt-sync")();
let num = Number(prompt("Escreva um número: "));
let fato;

function factorialize(num) {
  return num;
}
factorialize(num);
{
  num == fato;
}

console.log(fato);
