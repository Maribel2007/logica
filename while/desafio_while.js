const prompt = require("prompt-sync")();

let espaços = 0;
const caminho = 10;

While(true);
{
  const x = Math.random() * 20;
  const num = Math.ceil(random);

  if (num == 1) {
    espaços = espaços - 1;
  } else if (num == 20) {
    espaços = espaços + caminho;
  } else if (num >= 12) {
    espaços = espaços + 1;
  }

  if (espaços >= caminho) {
  }
  console.log("voce^ chegou no caminho e utilizou", i, "iterações");
  //    break
}

//não finalizado
